# Existing-project host handoff assessment

Candidate-only parent transcription of the read-only assessment by
`/root/modular_byte_handoff_acceptance`, task
`mes-change-handoff-design-20260918`, plan
`sha256:a58b453d9d2e004f79de1270f44a306d43b73ce60a77f0e66f226e9d3cc0bc0b`.
Bootstrap passed as `DPMBR-6F35796C40145239`. All 1,227 source snapshot files
remained unchanged at `848b0ca3914e8645a1219090eec315f5c3784a22`.

The released semantic Modules already support `design-change`,
`generate-contract-change`, and `decompose-change`. The missing behavior is in
Desktop host context preparation, materialization, input lists and activation.

## Execution continuity before another attempt

The actual completed byte-handoff construction attempt remains `prepared` in
continuity revision 2. Settle it only after proving its original claim, actual
native dispatch/terminal evidence and genuine zero-executor WorkExecution replay.
Record allowed transitions with CAS and durable journals, preserving every
original claim field. This settles execution-attempt evidence only: verification,
integration and the work completion ledger remain unchanged. Production
`settleRecordedDesktopDispatch` requires installed-coordinator receipts that this
construction bridge does not have; do not fabricate them.

## Host lineage contract

Use a versioned planning context that binds the previous approved head and its
verified Gate/replay, newly approved upstream artifacts and the derived planning
state. Keep the previous head current until its owning downstream Gate commits.
Validate both upstream authority and predecessor before effects and again before
activation; activation CAS must start from the verified predecessor. Do not
clear the head or activate an unapproved intermediate state.

Existing closed host schemas need new versions: architecture boundary/handoff/
materialization currently require discovery; contract planning permits only
establishment or not-applicable; work-context submission lacks prior work Gate,
current work baseline and ApprovedChangePackage; fixed four/seven-file handoffs
cannot carry change artifacts. Preserve historical v1 replay. New host schema
bytes require their owning contract assessment, while published Module operation
and state schemas remain unchanged.

## Route changes

Architecture context must load the previous approved architecture and activation,
current approved requirements/overview pair, explicit project context and
repository snapshot. Derive baselined state and let Core select `design-change`.
Discovery is not change authority. Guard initial invocation and response against
stale heads; activation must use the verified predecessor-head binding.

Contract planning must validate prior ContractGate replay/activation and retain
the exact baseline while deriving required intents from current architecture.
Execution must supply `currentBaseline`. Desktop checkpoint input verification
must derive its input roles from the selected route, including
`current-contract-baseline`; replace neither exact equality nor Gate authority.
Preserve explicit not-applicable routing and prior contract history.

Work context must accept a mutually exclusive baselined bundle containing current
work baseline, genuine prior work Gate/replay/activation, ApprovedChangePackage,
repository and capabilities. Its pre-change and target bindings, work baseline,
repository revision/tree, approval and graph references must resolve exactly.
Let Core select `decompose-change`, use current repository input and materialize
the added artifacts. Check previous work head as well as architecture/contracts
before dispatch and response. Existing owning work promotion/CAS remains intact.

## Verification

Use isolated durable tests for successful existing-project progression, stale or
substituted heads/bytes/Gates/ACP/repository, cloned replay receipts, unchanged
establishment/not-applicable behavior, interruptions around context/checkpoint/
graph/head publication and zero-adapter historical replay. Continuity settlement
must resume partial journals, reject changed evidence and avoid duplicate writes.

## Observed authority

- Architecture Gate: `sha256:f4f56fa4e0b5ae5022c7515a2760034885ff54a13bca8f19d7d6d29c495545b9`.
- Activated architecture state: `sha256:45396750d905b057ab50bd256072c3136e2e136dc40b3dc8946c5098b25df937`.
- Contract Gate: `sha256:740a248baf392b7ab61d57b64923d7dfc2455f8b37035f840a96c62419caf999`.
- Activated contract state: `sha256:663862f944616a0950e10a1b9e1b8ecddedaa28be9726417c465d184d4dade33`.
- Work Gate: `sha256:e78e67ea21df9ac8f4b9c17ff870109d350a5edff8b96869bc0a2d9658c3f5d6`.

The project architecture-state JSON is historical RP data; use the verified
durable MES head. Historical ACP-MES-001 authorizes RP-to-MES transition, not a
new change against today's work baseline. No assessment is Gate approval or
ProjectMemory promotion.
