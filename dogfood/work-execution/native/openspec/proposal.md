# WorkExecution proposal

Add one provider-neutral WorkExecution module that executes one Core-selected runnable work item per immutable isolated attempt through a version-pinned execution binding. It returns proposed changes and raw evidence only; verification and integration remain downstream.
