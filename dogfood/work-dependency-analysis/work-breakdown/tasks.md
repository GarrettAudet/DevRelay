# WorkDependencyAnalysis work breakdown

This bounded OpenSpec adapter artifact proposes planning work only. DevRelay owns validation, traceability projection, Gate progression, and baseline promotion.

1. Define the provider-neutral WorkDependencyAnalysis contracts and proposal port.
2. Build the immutable candidate snapshot and version-pinned context-slice boundary.
3. Implement the native structured dependency proposer.
4. Add optional Task Master and OpenSpec proposal bridges behind the proposal port.
5. Add the Spec Kit consistency-review bridge without granting graph authority.
6. Implement Core-owned static-DAG mechanics with Graphology-DAG.
7. Evaluate deterministic dependency policy through OPA.
8. Implement the WorkDependency Gate and exact DAG baseline promotion.
9. Add trusted forward-only dependency traceability projection.
10. Verify determinism, drift rejection, cycles, missing dependencies, ordering, and parallel boundaries.
11. Publish operator and extension documentation.

No item assigns specialists, executes work, changes code, or claims verification completion.
