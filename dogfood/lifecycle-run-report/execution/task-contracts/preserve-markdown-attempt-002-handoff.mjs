import { createHash } from "node:crypto";
import { readFileSync, writeFileSync } from "node:fs";

const bytes = Buffer.from('{ "apiVersion": "devrelay.dev/v1alpha1", "kind": "BootstrapWorkItemHandoff", "executionId": "WE-RUN-DOGFOOD-WI-RUN-MARKDOWN-ATTEMPT-002", "workItemId": "WI-RUN-MARKDOWN", "outcome": "pass", "changedFiles": [ "src/lifecycle-run-report-markdown.mjs", "test/lifecycle-run-report-markdown.test.mjs" ], "verification": [ { "command": "node --experimental-loader=file:///C:/Users/garre/OneDrive/Documents/Portable%20Best%20Practices/.tmp-devrelay-snapshot-dependency-loader.mjs --test test/lifecycle-run-report-markdown.test.mjs", "exitCode": 0, "summary": "5 focused tests passed, including reordered-input byte identity, digest-bound stage source links, and policy filtering of top-level and source links." }, { "command": "git diff --check -- src/lifecycle-run-report-markdown.mjs test/lifecycle-run-report-markdown.test.mjs", "exitCode": 0, "summary": "No whitespace errors were found in the authorized changes." } ], "evidence": [ { "kind": "lifecycle-run-report/markdown-tests", "relativePath": "test/lifecycle-run-report-markdown.test.mjs", "digest": "sha256:4e5aaa8022f23ff0d5032f4f3861c974117e8e12ecda3a2f92a7751917d80ea1" } ], "notes": "Resolved all three revision failures by excluding order-sensitive snapshot digests from primary Markdown bytes, deterministically sorting equivalent collections, rendering allowed stage sourceFacts as exact digest-bound links, and applying content policy independently to ledger, graph, artifact, traceability, and stage-source links.", "residualRisks": [ "none" ] }', "utf8");
const target = new URL("attempts/WI-RUN-MARKDOWN.attempt-002.handoff.raw.json", import.meta.url);
writeFileSync(target, bytes);
const stored = readFileSync(target);
if (!stored.equals(bytes)) throw new Error("raw handoff byte identity was not preserved");
console.log(`sha256:${createHash("sha256").update(stored).digest("hex")}`);
