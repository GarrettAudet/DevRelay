# RP-001 ReleasePreparation and ReleaseVerification tasks

- [ ] Implement closed release artifacts, contracts, and validators (`WI-RP-ARTIFACT-CONTRACTS`).
- [ ] Implement exact candidate identity and conditional lifecycle routing (`WI-RP-IDENTITY-ROUTING`).
- [ ] Implement native deterministic candidate materialization on Windows (`WI-RP-NATIVE-MATERIALIZATION`).
- [ ] Implement bounded release adapters and effect approvals (`WI-RP-ADAPTER-EFFECTS`).
- [ ] Implement stored-byte verification and versioned release policy (`WI-RP-VERIFY-POLICY`).
- [ ] Implement ReleaseVerificationGate and the compact readiness summary (`WI-RP-GATE-SUMMARY`).
- [ ] Implement trusted release traceability contribution (`WI-RP-TRACEABILITY`).
- [ ] Add complete security, recovery, determinism, telemetry, and regression coverage (`WI-RP-REGRESSION`).
- [ ] Run the clean installed-package Windows Desktop end-to-end release lifecycle without publishing (`WI-RP-WINDOWS-E2E`).

Dependency ordering in this file is advisory. WorkDependencyAnalysis owns the authoritative DAG.
