# ADR-DG1-001: Use bounded host-executor ArchitectureDesign adapters

Status: proposed

The host invokes exact versioned capabilities; Core retains routing, checkpoints, validation, Gate, and traceability authority.
