# DevRelay Greeting Card dogfood

A separate minimal Godot project used to prove the full DevRelay lifecycle through ChatGPT Desktop on Windows. Entering a name produces a deterministic greeting; blank input falls back to "friend".
