# SIM-001 bounded work proposal

This is the native OpenSpec Tasks proposal consumed by DevRelay WorkBreakdown. It is planning evidence only. DevRelay Core owns operation selection, normalization, validation, traceability contribution, and WorkBreakdownGate progression.

1. Implement immutable workflow profiles and deferred verification obligations.
2. Implement the eight-operation supported facade.
3. Enforce root, advanced, pack, and compat/v1 API tiers.
4. Implement SQLite lifecycle state and content-addressed artifact storage.
5. Implement isolated Git worktrees and deny-by-default grant enforcement.
6. Implement the exact Desktop executor binding and deterministic crash recovery.
7. Implement the Windows operator CLI and stable result/exit-code contracts.
8. Implement checksum-bound GitHub Release evidence assets and retrieval verification.
9. Migrate Godot/GdUnit4 to the generic optional-pack contract and prove cross-domain conformance.
10. Align 0.10.0-rc.2 preview metadata, compatibility guidance, and release-review documentation.
11. Repair transition-sensitive historical fixtures and add complete SIM-001 regression coverage.
12. Run a clean-checkout Windows Desktop end-to-end dogfood and publish bounded stacked review changes.

The proposal does not assign specialists, schedule work, establish authoritative dependencies, execute code, verify completion, integrate changes, publish a stable release, or approve BusinessAcceptance.
