# Pickup prompt

You are the implementation owner for DevRelay at
`https://github.com/GarrettAudet/DevRelay.git` on branch
`codex/lifecycle-run-report-completion`.

Read `CURRENT_STATUS.md` and every file under
`handoff/2026-08-11-controlled-release-acceptance-pickup/` before acting. The
verified controlled-source target is exact commit
`477e7a449cb90d4ecb86c7271cb59f3e2d09b0d6`. Local release verification is
green (842 tests, 840 pass, 0 fail, 2 skip), and GitHub Actions run
`31497854653` passed Node 20/22 on Windows/Ubuntu.

Resume at the executable final SystemVerification/BusinessAcceptance boundary.
The unfinished candidate materializer is preserved as
`candidate-materializer.wip.txt`; audit and finish it, but do not treat it as
execution evidence. Use the real released APIs and contributors, prove
zero-call checkpoint and graph replay, derive all 81 technical-coverage paths,
and evaluate all 8 objectives, 9 metrics, and 32 business-scope identities.

The owner previously approved those counts, two `designed-by` links to
`EL-DEVRELAY-CORE`, and the controlled Windows/Desktop source boundary, but the
exact approval named superseded commit `33e65ba`. After producing the corrected
candidate, present its exact target commit, semantic digest, raw digest,
technical-coverage digest, graph checkpoint, and exclusions for one new exact
approval. Only then invoke BusinessAcceptanceGate and merge acceptance
traceability.

Do not claim public npm publication, a one-click Desktop plug-in, a hosted
backend, deployment, or production operation. Stop on drift, missing evidence,
non-zero replay calls, incomplete paths, or blocking graph diagnostics. Finish
by updating status/release/handoff evidence, running the full release gate,
committing, pushing, and verifying the final four-job matrix.
