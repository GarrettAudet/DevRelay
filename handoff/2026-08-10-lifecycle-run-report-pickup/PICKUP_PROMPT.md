# Ready-to-paste implementation-owner prompt

```text
Work only in the saved DevRelay repository on branch
codex/v0.5-lifecycle-run-report for increment
DGI-LIFECYCLE-RUN-REPORT-2026-08-10. Read AGENTS.md and every file under
handoff/2026-08-10-lifecycle-run-report-pickup/ before acting. Treat the
handoff as a status projection and resolve authority using
DECISIONS_AND_INVARIANTS.md.

Resume DG-1 at the portable ContractGeneration boundary. The integrated Route B
candidate is CCS-DC7A978C15992619 with raw digest
sha256:361173cb70a33c2631daef341512cd5115f806b25cd9a48b759cc612968e2d2b.
Exactly 48 approved interface intents require generation. Keep the nine
WorkBreakdown/WorkDependencyAnalysis interfaces excluded because their nested
interface.contractGeneration.required disposition is false.

First snapshot branch/HEAD/tree/divergence/index and all dirty bytes. Preserve
every unrelated byte. Correct only the portability defect:
dogfood/bootstrap-contract-generation-host-executor/materialize-dg1.mjs still
accepts an external continuation directory. Make its immutable input
repository-relative and content-addressed. Do not document or introduce a
developer-machine path, hidden environment context, baseline mutation, or
rewritten historical evidence.

Update the exact bounded contract/evidence and focused fixtures, independently
verify the changed boundary, and perform exact checkpoint-bound
ContractGeneration replay. Prove the 20-entry manifest, 48 selection, one
generator call, one checkpoint, zero replay calls, byte-identical replay, and
terminal digest equality. Then stop and present the candidate for a separate
ContractGate decision. ContractGate promotion is not authorized.

Do not implement downstream modules, close PB items out of order, mutate Gate,
baseline, TraceabilityGraph, completion, accepted-prefix, or PB authority, or
stage/commit/push without separate authorization. Fail closed on digest drift,
missing evidence, overlapping dirty deltas, count changes, environment-only
limitations, or any request for implicit authority. Report exact changed paths,
commands, results, limitations, and the next authority boundary.
```
