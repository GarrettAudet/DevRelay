# Pickup prompt

Continue DevRelay from branch `codex/pm-001-project-memory`. Read `CURRENT_STATUS.md`, `project/CurrentSynopsis.md`, `project/project-memory-baseline.json`, and this handoff package before acting. PM-001 is already implemented, integrated, system-verified, business-accepted, and concluded. Your job is only to regenerate the final catalog on the complete staged branch candidate, run the clean canonical and installed-package gates, evidence-seal and push the branch, and preserve protected `main` as promotion authority. If the branch tip is already sealed and green, do not repeat implementation; report the exact commit and checks. The next product increment is EnvironmentPreparation/Verification and must run through the complete current DevRelay circuit with ProjectMemory loaded first.