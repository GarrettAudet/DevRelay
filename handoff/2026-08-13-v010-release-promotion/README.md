# DevRelay v0.10 release-promotion pickup

This package is the authoritative pickup point for finishing the DevRelay
`0.10.0-rc.1` GitHub source/library release.

The engineering lifecycle is complete through BusinessAcceptance. The only
remaining work is deterministic candidate stabilization and protected-main
promotion. Start with `CURRENT_STATE.md`, then follow `NEXT_ACTIONS.md`.

Do not broaden the release boundary or rerun upstream product design unless a
gate exposes a material contradiction.
