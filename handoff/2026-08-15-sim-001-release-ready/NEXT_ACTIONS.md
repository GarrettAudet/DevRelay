# Next actions

1. Confirm the current tip is the direct evidence-seal child of `53760e86617fcc28286a916200866448a441d4c8`.
2. Run `npm.cmd run release:check`; require zero failures.
3. Push `codex/sim-001-simplification` and promote through protected `main`.
4. Publish the GitHub prerelease and checksum-bound assets if desired; do not claim public npm publication.
5. Start RM-001 through the complete released circuit, beginning with the visible RequirementsGathering interview and Gate.
6. Implement mandatory `DevRelaySessionBootstrap` before RoadmapManagement can be called release-ready.
