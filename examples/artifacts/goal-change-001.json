{
  "apiVersion": "devrelay.dev/v1alpha1",
  "kind": "GoalArtifact",
  "goalId": "goal-change-001",
  "statement": "Require policy-driven multi-factor authentication for protected sessions.",
  "objectives": [
    "Require every authentication factor selected by the applicable security policy."
  ],
  "constraints": [
    "Do not select an application architecture during requirements gathering."
  ],
  "acceptanceCriteria": [
    "A policy-bound user cannot establish a session using fewer factors than policy requires."
  ],
  "assumptions": [
    "The product already has a persistent user identity."
  ]
}
