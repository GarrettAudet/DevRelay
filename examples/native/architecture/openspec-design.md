# Authentication hardening design

Deterministic DevRelay conformance fixture for the bounded OpenSpec design-change handoff.

The change preserves the authentication boundary while introducing rate-limit intent and updated architectural evidence.
