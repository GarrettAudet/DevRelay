# Authentication Change Tasks

This bounded OpenSpec `tasks.md` artifact proposes only the authorized WorkBreakdown delta. DevRelay remains responsible for validation, traceability contribution, gating, and progression.

- [ ] Update `WI-AUTH-CORE` to add the approved audit-safe authentication behavior while retaining its stable identity.
- [ ] Add `WI-AUTH-FAILURE-VERIFICATION` as the replacement negative security-test deliverable.
- [ ] Retire `WI-AUTH-FAILURE-TESTS` because its bounded scope is replaced by `WI-AUTH-FAILURE-VERIFICATION`.
- Preserve `WI-AUTH-OPERATIONS` and `WI-AUTH-PERFORMANCE` unchanged; they are outside this authorized delta.

These entries are planning statements. They do not execute code, assign a specialist, establish an authoritative dependency DAG, or claim verification.
