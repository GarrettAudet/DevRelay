interface:
  display_name: "DevRelay Cycle"
  short_description: "Run one bounded DevRelay engineering cycle"
  default_prompt: "Use $devrelay-cycle to execute one bounded improvement through the released DevRelay lifecycle."
