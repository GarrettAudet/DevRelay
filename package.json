{
  "name": "devrelay",
  "version": "0.0.0",
  "private": true,
  "description": "A deterministic runner for composable software-engineering modules.",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
